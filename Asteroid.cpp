#include "Asteroid.h"


void InitAsteroid()
{
    float positionX = GetRandomValue(-GetScreenWidth()/30,GetScreenWidth());
    float positionY = GetRandomValue(-GetScreenHeight()/30,GetScreenHeight());

    Vector2 position = {positionX, positionY};
    //Vector2 direction = ;
    
}
